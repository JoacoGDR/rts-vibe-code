// Package config holds the typed configuration loaded from environment
// variables for every binary mode. Modes share most fields; mode-specific
// defaults live alongside the [Mode] constants.
package config

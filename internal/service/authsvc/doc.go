// Package authsvc owns the registration, login and WS-ticket flows. It
// declares the repository interfaces it consumes (Go's "consumer defines
// the interface" idiom) and depends only on domain types + pkg/.
package authsvc

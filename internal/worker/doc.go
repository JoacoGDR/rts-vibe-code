// Package worker is the composition root for the worker binary mode. It
// hosts the background jobs (Phase 1 ships only the snapshot worker;
// Phase 4+ adds AI takeover, macro-pulse and notifier workers).
package worker

name: Deploy backend

# Scaffolded for Phase 7. Disabled until infra is provisioned and ECR/EKS are
# wired up. Trigger is workflow_dispatch only so it never runs by accident.

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag to deploy"
        required: true
        default: latest

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    if: false # Phase 7 will flip this on
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build and push
        run: |
          docker build -t $ECR_REGISTRY/supremacy:${{ inputs.image_tag }} .
          docker push $ECR_REGISTRY/supremacy:${{ inputs.image_tag }}

  rollout:
    if: false # Phase 7
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Update K8s deployment
        run: |
          aws eks update-kubeconfig --name supremacy --region us-east-1
          kubectl set image deployment/core-api supremacy=$ECR_REGISTRY/supremacy:${{ inputs.image_tag }}
          kubectl set image deployment/gateway supremacy=$ECR_REGISTRY/supremacy:${{ inputs.image_tag }}
          kubectl set image statefulset/engine supremacy=$ECR_REGISTRY/supremacy:${{ inputs.image_tag }}
          kubectl set image deployment/worker supremacy=$ECR_REGISTRY/supremacy:${{ inputs.image_tag }}
